# Config pública de despliegue (no es secreto). La usa `next build` en producción
# para que el tab Live apunte al dispatcher real de Render (wss://).
# En local/dev el Dockerfile y el valor por defecto (http://localhost:8000) siguen reinando.
NEXT_PUBLIC_DISPATCHER_URL=https://recaudo-t-dispatcher.onrender.com